# 爪爪大乱斗英雄辐射对比工具 - 需求文档

## 一、产品概述

一个用于对比自走棋类游戏（如爪爪大乱斗）中英雄辐射格子效果的网页工具。

用户可以在 **5列 × 4行** 的棋盘上设置英雄位置和辐射范围，实时查看每个格子被辐射的次数，支持多英雄管理、对比统计、数据导入导出。

---

## 二、棋盘规格

- **5列 × 4行 = 20 个格子**
- 坐标表示：(列, 行)，列 0-4，行 0-3

```
(0,0) (1,0) (2,0) (3,0) (4,0)
(0,1) (1,1) (2,1) (3,1) (4,1)
(0,2) (1,2) (2,2) (3,2) (4,2)
(0,3) (1,3) (2,3) (3,3) (4,3)
```

---

## 三、核心功能

### 1. 双棋盘布局

页面分为左右两个棋盘：

| 棋盘 | 位置 | 作用 |
|------|------|------|
| 结果棋盘 | 左侧 | 展示辐射计算结果，每个格子显示被辐射的次数 |
| 选择棋盘 | 右侧 | 用户交互区域，选择英雄位置和辐射范围 |

### 2. 英雄位置选择

- 在右侧选择棋盘中，点击任意格子设置为英雄位置
- 英雄位置用特殊样式高亮（如金色/橙色标记）
- 只能选择一个位置作为当前编辑英雄的位置

### 3. 辐射范围设置

**两种设置方式：**

#### 方式一：预设模板
提供常见的辐射模式供快速选择：
- 十字1格（上下左右各1格）
- 十字2格（上下左右各2格）
- 方形1圈（周围8格，九宫格去掉中心）
- 方形2圈（周围两圈）
- 仅上下
- 仅左右
- 其他常用模式

#### 方式二：自定义
- 用户手动点击格子来勾选/取消辐射范围
- 辐射范围相对于英雄位置
- 选择棋盘上实时预览辐射效果

### 4. 辐射计算

#### 计算规则
- 英雄放置在某个格子上，其辐射范围内的格子计数 +1
- 超出棋盘边界的辐射范围自动忽略（不算）
- 多个英雄叠加时，同一格子被多个英雄辐射则次数累加

#### 示例
> 英雄放在左上角 (0,0)，辐射范围是"十字1格"（上下左右各1格）
> - 上方：越界，忽略
> - 左方：越界，忽略
> - 右方：(1,0)，有效
> - 下方：(0,1)，有效
> - 结果：有效辐射格子数 = 2

### 5. 结果展示

左侧结果棋盘显示：
- 每个格子上的数字 = 被辐射的次数
- 格子背景颜色随次数变化（次数越多颜色越深/越亮）
- 鼠标悬停格子，显示具体被哪些英雄辐射

### 6. 最佳位置计算

自动计算某个英雄放在棋盘哪个位置时，辐射的格子数最多：
- 穷举棋盘 20 个格子，找出辐射数最大的位置
- 显示最大辐射数量
- 可能有多个位置并列最佳，全部列出

---

## 四、英雄卡片管理

### 1. 英雄卡片

每个英雄保存为一张卡片，包含：
- 英雄名称
- 英雄位置（棋盘上的坐标）
- 辐射范围（偏移模式）
- 模式名称（如"十字1格"）
- 颜色标记（方便区分不同英雄）
- 等级（可选）

### 2. 卡片操作

- **添加**：创建新的英雄卡片
- **编辑**：修改已有英雄的位置、辐射范围、名称等
- **删除**：移除英雄卡片
- **复制**：基于现有英雄快速创建新英雄
- **选中/取消选中**：勾选的英雄参与叠加计算和对比

### 3. 卡片列表

- 左侧展示所有英雄卡片（可滚动）
- 支持多选（勾选多个英雄同时查看叠加效果）
- 悬停卡片时，结果棋盘上高亮显示该英雄的辐射范围
- 搜索框：英雄多了可以按名称搜索
- 排序：可按创建时间、名称、辐射效率等排序

---

## 五、统计与对比

### 1. 实时统计面板

显示当前选中英雄的综合数据：
- 总辐射格子数（有多少格子至少被辐射1次）
- 最高辐射次数（单个格子最多被辐射几次）
- 平均辐射次数

### 2. 英雄对比表格

将选中的英雄按列表形式对比展示：

| 英雄名称 | 辐射模式 | 放置位置 | 有效辐射数 | 理论最大值 | 效率 | 最佳位置辐射数 |
|---------|---------|---------|-----------|-----------|------|--------------|
| 英雄A   | 十字1格 | (0,0)   | 2         | 4         | 50%  | 4            |
| 英雄B   | 方形1圈 | (2,1)   | 8         | 8         | 100% | 8            |

- 支持点击列标题排序
- 效率 = 有效辐射数 / 理论最大值 × 100%

---

## 六、导入导出

### 导出功能
- 可以导出全部英雄，或仅导出选中的英雄
- 导出为 JSON 文件下载到本地
- 文件名可自定义，默认带时间戳

### 导入功能
- 选择本地 JSON 文件导入
- 导入时可选择：
  - **合并**：导入的英雄追加到现有列表中
  - **替换**：清空现有列表，用导入的内容替换
- 导入前显示预览，确认后再执行

---

## 七、界面布局

### 整体结构：三栏式

```
┌─────────────────────────────────────────────────────────────────┐
│  顶部：标题 + 导入按钮 + 导出按钮 + 设置/主题切换                 │
├──────────┬──────────────────────────┬──────────────────────────┤
│          │                          │                          │
│  英雄    │     左侧：结果棋盘        │    右侧：选择棋盘        │
│  卡片    │     (5×4 辐射数热力图)    │    (5×4 交互选择)       │
│  列表    │                          │                          │
│          │                          │                          │
│  (可     │    下方：统计面板         │   下方：英雄设置表单     │
│   滚动)  │    (总格子数/最高次数等)  │   (名称/模式/位置/保存)  │
│          │                          │                          │
└──────────┴──────────────────────────┴──────────────────────────┘
```

### 页面元素说明

**顶部导航栏**
- 工具名称
- 导入按钮
- 导出按钮（全部导出 / 选中导出）
- 主题切换（明/暗）

**左栏：英雄卡片列表**
- 搜索框
- "+ 添加英雄" 按钮
- 英雄卡片列表（每张卡片带勾选框、编辑、删除、复制按钮）
- 底部显示当前选中数量

**中栏：结果展示区**
- 5×4 结果棋盘（数字 + 颜色渐变）
- 统计面板（总辐射格子、最高辐射次数、平均次数）
- 英雄对比表格（展开/收起）

**右栏：选择与设置区**
- 5×4 选择棋盘（点击选位置，辐射范围预览）
- 辐射模式选择（预设模板下拉 + 自定义切换）
- 英雄信息表单（名称输入、颜色选择、等级选择）
- 操作按钮：「保存英雄」「重置」

---

## 八、交互细节

1. **实时计算**：修改英雄或切换选中状态时，结果自动更新，不需要点"计算"按钮
2. **悬停联动**：鼠标悬停英雄卡片 → 结果棋盘高亮该英雄的辐射范围
3. **多选叠加**：勾选多张英雄卡片 → 结果棋盘显示叠加后的辐射次数
4. **编辑模式**：点击卡片的"编辑"按钮 → 右侧选择棋盘加载该英雄的数据进行修改
5. **最佳位置提示**：编辑英雄时，显示该辐射模式下的最佳位置和最大辐射数
6. **颜色区分**：每个英雄分配不同颜色，叠加结果中可用颜色透明度表示来源

---

## 九、数据说明

### 需要保存的数据
- 所有英雄卡片（名称、位置、辐射模式、颜色、创建时间等）
- 用户界面设置（主题、布局偏好等）

### 存储方式
- 数据保存在浏览器本地（刷新页面不丢失）
- 通过导入导出功能可以跨设备迁移

---

## 十、功能优先级

### 第一阶段（基础功能）
1. 5×4 棋盘渲染
2. 单英雄位置选择
3. 辐射范围选择（预设模板 + 自定义）
4. 辐射数量计算与结果显示
5. 英雄保存与列表展示
6. 多英雄叠加计算
7. 基础统计数据

### 第二阶段（完善功能）
8. 最佳位置计算
9. 英雄编辑、删除、复制
10. 导入导出 JSON
11. 英雄对比表格
12. 悬停高亮联动

### 第三阶段（体验优化）
13. 卡片拖拽排序
14. 搜索与筛选
15. 主题切换
16. 撤销/重做操作
17. 分享链接（通过 URL 参数传递数据）
